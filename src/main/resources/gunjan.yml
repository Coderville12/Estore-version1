server:
  port: 9090
  
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/electro_store
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  servlet:
      Multipart:
          max-file-size: 10MB   
          max-request-size : 10MB
user:
    profile:
      image:
        path : images/users/
        
        
        
         